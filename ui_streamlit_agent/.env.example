# Streamlit Frontend Configuration
# This file contains ONLY frontend-facing variables.
# Database, Redis, and backend internal credentials are NOT stored here.

# Backend API Configuration
BACKEND_URL=http://api:8000
API_TIMEOUT_SECONDS=30

# Debug Mode (for development)
DEBUG=true

# LLM Display (backend-driven)
# The active provider and model are configured
# on the backend via environment variables such as:
# - LLM_PROVIDER
# - OPENAI_MODEL_NAME / ANTHROPIC_MODEL_NAME / GOOGLE_MODEL_NAME
# This frontend only reads and displays that configuration.

# Optional: Default credentials for demo (overridable in UI)
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=admin
